# Dev Kumar — Systems & OS Security Portfolio

An interactive, responsive portfolio shaped around containment, mandatory access control, policy engines, kernel and userland systems, Apple platform security, and pragmatic security adoption.

## Local development

```bash
npm install
npm run dev
```

## Validation

```bash
npm test
npm run lint
npm run pages
```

GitHub Pages deployment runs automatically from the `main` branch.
